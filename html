<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monitoramento de Equipamentos</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="container">
        <h1><i class="fas fa-file-alt"></i> Relatório Técnico</h1>
        
        <div class="datetime" id="datetime"></div>

        <div class="form-section">
            <div class="input-row">
                <div class="input-group">
                    <label for="responsible"><i class="fas fa-user"></i> Responsável:</label>
                    <input type="text" id="responsible" required>
                </div>
                
                <div class="input-group">
                    <label for="equipment-select"><i class="fas fa-cogs"></i> Equipamento:</label>
                    <select id="equipment-select" required>
                        <!-- Opções mantidas iguais -->
                    </select>
                </div>
            </div>

            <div class="input-row">
                <div class="input-group">
                    <label for="pressure"><i class="fas fa-tachometer-alt"></i> Pressão (bar):</label>
                    <input type="number" id="pressure" step="0.01" required>
                </div>
                
                <div class="input-group">
                    <label for="temperature"><i class="fas fa-thermometer-half"></i> Temperatura (°C):</label>
                    <input type="number" id="temperature" step="0.1" required>
                </div>
                
                <div class="input-group">
                    <label for="operation-select"><i class="fas fa-power-off"></i> Estado:</label>
                    <select id="operation-select" required>
                        <option value="parado">Parado</option>
                        <option value="em operação">Em Operação</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="photo-section">
            <div class="photo-upload" onclick="document.getElementById('photoInput').click()">
                <i class="fas fa-camera"></i>
                <span>Clique para adicionar foto</span>
                <input type="file" id="photoInput" accept="image/*" hidden>
            </div>
            <img id="photoPreview" class="hidden">
        </div>

        <button class="generate-btn" onclick="generatePDF()">
            <i class="fas fa-file-pdf"></i> Gerar Relatório PDF
        </button>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.4.0/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <script src="script.js"></script>
</body>
</html>
